<h1>Listing users_linemanagers</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users_linemanagers.each do |users_linemanager| %>
      <tr>
        <td><%= link_to 'Show', users_linemanager %></td>
        <td><%= link_to 'Edit', edit_users_linemanager_path(users_linemanager) %></td>
        <td><%= link_to 'Destroy', users_linemanager, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Users linemanager', new_users_linemanager_path %>
