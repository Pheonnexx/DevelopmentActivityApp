<%= form_for(@role_skill) do |f| %>
  <% if @role_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_skill.errors.count, "error") %> prohibited this role_skill from being saved:</h2>

      <ul>
      <% @role_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
